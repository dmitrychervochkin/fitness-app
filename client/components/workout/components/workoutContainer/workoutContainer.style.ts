import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        gap: 40,
        marginBottom: 200,
    },
    actions: {
        gap: 10,
        paddingHorizontal: 20,
    },
    cardsContainer: {
        gap: 20,
    },
});
